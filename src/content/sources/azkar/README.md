# Azkar Authoring Source

- عدّل `manifest.js` لإضافة تصنيف جديد.
- عدّل ملفات `categories/*.js` لإضافة ذكر جديد أو تعديل تصنيف قائم.
- بعد أي تعديل شغّل: `npm run azkar:split && npm test && npm run build`.
- لا تعدّل `src/content/azkar/generated/` يدويًا.
