# Duas Authoring Source

- عدّل `manifest.js` لإضافة تصنيف جديد.
- عدّل ملفات `categories/*.js` لإضافة دعاء جديد أو تعديل تصنيف قائم.
- بعد أي تعديل شغّل: `npm run duas:split && npm test && npm run build`.
- لا تعدّل `src/content/duas/generated/` يدويًا.
